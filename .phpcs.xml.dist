<?xml version="1.0"?>
<ruleset name="qtype_accounting">
    <description>PHP CodeSniffer configuration for qtype_accounting.</description>

    <file>.</file>

    <exclude-pattern>amd/build/*</exclude-pattern>
    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>node_modules/*</exclude-pattern>

    <rule ref="moodle"/>
    <rule ref="moodle-extra"/>
</ruleset>
